# Pantrix Library

Shared server-side utilities for the Pantrix app.

- `db.ts` — Prisma client singleton
- `auth.ts` — JWT session helpers (login, verify, cookies)
- `serializeRecipe.ts` — Map DB recipe rows to API shape
- `recipes.ts` — Indian recipe catalog queries + pantry matching
