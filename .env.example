# Local development (SQLite for quick local testing)
DATABASE_URL="file:./dev.db"

# Production deployment (Vercel / Render / Railway / Supabase / Neon):
# Use a real PostgreSQL connection string here.
# DATABASE_URL="postgresql://user:password@host:5432/pantrix?sslmode=require"

# JWT secret for auth cookies (generate a random string for production)
JWT_SECRET="change-this-to-a-long-random-string-in-production"
